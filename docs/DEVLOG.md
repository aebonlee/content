# 개발일지 — contents (AI 홍보 스튜디오)

## 2026-06-13 · 갤러리 9개로 — 종합 산출물 2종 추가(3×3 균형)

기존 7개라 3열 그리드 마지막 줄에 1개만 남던 비율 문제. 모듈을 가로지르는 종합 산출물
**g8 나만의 프롬프트 세트**(5요소·70실습·SCORE) · **g9 홍보 자동화 포트폴리오**(전체 결과물 묶음)
2종 추가 → 3×3 꽉 참. 이 둘은 `module` 대신 `badge`(PROMPT/FINAL)·`sub`를 쓰고,
렌더링은 `g.badge || M{module}` / `g.sub || M{module}.제목`으로 분기. GalleryArt에 SVG 2종 신규.
갤러리 리드 문구도 9개에 맞게 수정.

## 2026-06-13 · 핵심 숫자 강조 띠 + 히어로 CTA 다듬기

히어로 바로 아래에 `stat-band` 추가 — 8시간 · 7개 모듈 · N개 세션 · 70개 실습.
숫자는 `practiceExamples`/`modules`에서 실제 집계(하드코딩 X). 큰 mono 숫자(pigment)에
amber 단위, 칼럼 사이 세로 구분선, ≤560px 2열. 히어로 CTA에 아이콘(book-open·stamp) 추가.

## 2026-06-13 · 히어로 일러스트 — 결과물 떠다니는 구성으로 교체

기존 "청사진 위 새순" 단일 SVG → 콘텐츠 카드·이미지 카드·랜딩 페이지 카드가
청사진 궤도 위를 **부드럽게 떠다니는** 구성으로 재설계. 카드마다 다른 주기/위상의
`heroFloat` 애니메이션(4.8~7.4s), `prefers-reduced-motion`이면 정지.
하트칩·반짝임 등 작은 강조 요소로 생동감 추가. 색은 토큰 변수로 팔레트 유지.

## 2026-06-13 · 메인페이지 갤러리 미리보기도 SVG 일러스트로 통일

홈(`Home.jsx`)의 갤러리 미리보기 섹션이 아직 FA 이모지 플레이스홀더를 쓰고 있어,
갤러리 페이지와 동일하게 `GalleryArt`로 교체. 메인↔갤러리 비주얼 일관성 확보.
(갤러리 도구 뱃지는 알약형이 아닌 텍스트라 색 태그는 미적용 — 실습예제/세션 도구칩에만.)

## 2026-06-13 · 갤러리 "당신이 만들게 될 것들" — 결과물 SVG 일러스트

기존 갤러리 커버는 FA 아이콘 1개짜리 플레이스홀더였음. `components/GalleryArt.jsx`를 새로 만들어
7개 결과물을 **팔레트 SVG 일러스트**로 직접 그림(청사진 톤·모눈 배경). 색은 토큰 변수
(`var(--pigment/ink/accent/wash)`)를 그대로 써 다크/라이트 테마와 같은 팔레트로 호흡.
- g1 진단표(표+체크+돋보기) · g2 기획 한 장(문서+과녁) · g3 콘텐츠 세트(겹친 카드+하트)
- g4 시각자료(이미지 프레임+해+언덕) · g5 UI 시안(와이어프레임) · g6 랜딩(브라우저+히어로+CTA)
- g7 자동화(노드 흐름+톱니)
`.gallery-card__art`로 커버를 꽉 채우고 호버 시 살짝 확대.

## 2026-06-13 · 도구 뱃지 ChatGPT/Claude 색 구분

실습예제 뱃지(`.ex-card__tool`)와 세션 도구칩(`.tool-chip`)이 모두 같은 pigment 색이라
ChatGPT·Claude가 한눈에 구분되지 않던 문제. `lib/tools.js`의 `toolTag()`로 이름→클래스 매핑하고
`.tag-gpt`(초록 #10a37f) · `.tag-claude`(클레이 #d97757) · `.tag-both`(그라데이션) 추가.

## 2026-06-13 · 프롬프트 5요소 범례 — 전체 가로폭 카드로 재디자인

`.lecture__head`의 `max-width: 46rem`가 5요소 그리드까지 묶어 카드가 좁게 눌려 있었음.
제목·리드는 가독성을 위해 `.lecture__head-intro`로 분리해 46rem 유지하고, **5요소는 컨테이너
전체 가로폭**으로 해방. 카드는 세로 구성(아이콘 타일 → 번호 → 키워드 → 설명)으로 다시 그려
호버 시 살짝 떠오르고 상단에 pigment→amber 그라데이션 라인이 비치게 함.
반응형: ≤980px 3열 · ≤560px 2열로 자동 줄바꿈.

## 2026-06-13 · 실습예제 첫 화면에서 본문이 안 보이던 버그 수정

**증상** — `/examples`(실습예제) 진입 시 스크롤하기 전까지 좌측 모듈 메뉴만 보이고,
우측 본문(실습 예제 10개)이 비어 보임.

**원인** — `Examples.jsx`의 본문 전체(`.lecmod`, 카드 10개)가 단일 `<Reveal>`로 감싸여 있는데,
`Reveal`의 IntersectionObserver `threshold: 0.15`(요소의 15% 노출 시 발동)가 문제.
본문이 세로로 매우 길어 첫 화면에서 노출 비율이 15%에 못 미쳐 `.in`이 붙지 않고,
`.reveal { opacity:0 }` 상태로 숨은 채 남았다. Reveal로 감싸지 않은 좌측 rail만 보였던 것.

**수정** — `Reveal`의 옵저버를 `{ threshold: 0, rootMargin: '0px 0px -40px 0px' }`로 변경.
요소가 조금이라도 화면에 들어오면 바로 나타나, 긴 요소도 안전. 전역 컴포넌트라 잠재 버그를 근본 수정.

## 2026-06-11 · 프로젝트 완료 요약 (개발 종료)

### 개요
휴넷 인재키움 과정 **「AI로 만드는 홍보 실무 — 콘텐츠·이미지·페이지까지 자동화 완성」**
(인재키움 프리미엄 · 단과 초급 · **오프라인 집합교육 8시간**)의 **대면 강의 학습자료** 웹사이트.
`templete03` 베이스에서 시작해 과정 전용 학습 플랫폼으로 고도화.

- 리포: `aebonlee/contents` · 도메인: `contents.dreamitbiz.com`
- 스택: React 19 · Vite 7 · React Router 7 · Font Awesome 6.5.2 (DB/인증 없는 **정적 SPA**)
- 배포: GitHub Actions → **gh-pages 브랜치**(`peaceiris/actions-gh-pages`, main push 자동)
- 상태 저장: 도장(이해 현황)만 `localStorage` + 커스텀 이벤트로 페이지 간 동기화

### 정보구조 — 상단 메뉴 8종
1. **About** — 과정 소개·기대효과·대상 + 일정 + 준비물
2. **커리큘럼** — 공식 7개 모듈(M1~M7) · 8교시(50분 단위) 타임테이블
3. **학습강의안**(`/lecture`) — 마스터-디테일. 좌측 교시 클릭 → 우측 인라인 상세
   (개념·실습절차·프롬프트 예시·흔한 실수·도구·체크리스트). **모듈 하단 "이해 완료" 도장 버튼**
4. **실습예제**(`/examples`) — 7모듈 × 10 = **70개**, 프롬프트 5요소 완성형
5. **프롬프트 학습 & 평가**(`/prompt`) — 5요소 학습 + **SCORE 채점 실습**(점수·등급·피드백)
6. **갤러리** — 7개 모듈 결과물 쇼케이스
7. **도장깨기** — 7개 모듈 이해 현황판(강의안과 연동)
8. **커뮤니티** — 채널 안내 + FAQ 아코디언

### 커리큘럼 (공식 7모듈, 합 8H)
M1 업무효율화의 이해(1교시) · M2 홍보 기획·프롬프트(2교시) · M3 콘텐츠 작성(3교시) ·
M4 시각자료 제작(4교시) · M5 UI 시안 Google Stitch(5교시) · M6 페이지 구성(6교시) ·
**M7 반복업무 자동화(7~8교시 · 2H)**.

### 핵심 설계 결정
- **프롬프트 5요소**(역할·맥락·과제·제약·출력형식)를 실습예제·프롬프트평가의 **공통 기준**으로 통일.
- **도장 = 이해도 체크**: 도장깨기 페이지가 아니라 강의안 학습 후 하단 버튼으로 획득,
  클릭 시 사라지지 않고 **색·문구 변화**(토글), 현황판에 실시간 반영.
- 동영상 시청형이 아닌 **대면 워크숍 학습자료** 컨셉(영상 플레이어 없음).
- 이모지 대신 **Font Awesome** 아이콘 일관 사용.

### 데이터/엔진 위치 (콘텐츠 수정 지점)
- `src/config/site.js` — 과정·일정·모듈·도장·갤러리·네비
- `src/config/lectures.js` — 학습강의안 상세 본문
- `src/config/examples.js` — 실습예제 70개(5요소 프롬프트)
- `src/config/promptLab.js` — 프롬프트 평가 기준·시나리오·SCORE 채점 엔진(`evaluatePrompt`)
- `src/lib/stamps.js` — 도장 공유 상태(`useStamps`/`toggleStamp`)

### 남은 TODO
- [ ] 실제 강의 자료/결과물 이미지·도구 스크린샷 교체
- [ ] (선택) 도구별 단계별 캡처 가이드 보강

---

## 2026-06-11 · 도장찍기를 강의안 하단 버튼과 연동

- 도장을 도장깨기 페이지에서 찍지 않고, **각 학습강의안 모듈 하단의 "이해 완료" 버튼**으로 찍도록 변경.
- 클릭 시 사라지지 않고 **색·문구 변화**로 획득 표시(버튼 primary→accent, 패널 강조, 문구 토글). 재클릭 시 해제.
- 공유 상태 모듈 `src/lib/stamps.js`(localStorage + 커스텀 이벤트 `useStamps`)로 페이지 간 실시간 동기화.
- 도장깨기 페이지는 **현황판**으로 전환: 획득=도장 배지(색), 미획득=해당 강의안으로 이동 링크. 토글 버튼 제거(초기화는 유지).

## 2026-06-11 · 프롬프트 학습 & 평가 메뉴 신설 (SCORE 채점)

- 실습예제 옆에 **프롬프트 학습 & 평가**(`/prompt`) 추가(메뉴 8개).
- (참고: data 프로젝트 dasco의 prompt-evaluation/PromptPractice 패턴)
- **학습부**: 프롬프트 5요소(역할·맥락·과제·제약·출력형식, 각 20점) 기준 카드 +
  등급표(S~D) + 핵심 기법 5종 + 개선 전/후 채점 예시.
- **평가 실습부**: 홍보 실무 시나리오 6종 → 프롬프트 직접 작성 → **SCORE 채점 엔진**
  (키워드·구조·길이 휴리스틱, 클라이언트 사이드)으로 5요소 0~20점·등급·개선 피드백 산출 +
  요소별 점수 막대 + 모범답안 토글.
- 데이터·엔진 `src/config/promptLab.js`(`evaluatePrompt`), 페이지 `PromptLab.jsx`.
- 검증: 모범답안 90/S, 약한 프롬프트 16/D로 변별.

## 2026-06-11 · 실습예제 메뉴 신설 (모듈별 10개)

- 상단 메뉴 "학습강의안" 다음에 **실습예제**(`/examples`) 추가(총 7개 메뉴).
- 학습강의안과 동일한 마스터-디테일: 왼쪽 모듈 선택 → 오른쪽에 그 모듈의 실습 예제 10개.
- 각 예제: 번호·제목·실습 과제·복붙용 프롬프트·도구. **7모듈 × 10 = 70개**.
- 데이터 `src/config/examples.js`(`practiceExamples`). 라우트 `/examples/:moduleId`.

## 2026-06-11 · 학습강의안 마스터-디테일 전환

- 별도 상세 페이지로 "또 클릭해 들어가는" 방식 제거 → **한 페이지 마스터-디테일**.
- 왼쪽 TIMELINE(교시) 클릭 → 오른쪽에 그 교시 상세 강의안이 즉시 표시(인라인).
- "강의안 열기" CTA 삭제, 활성 교시 하이라이트 추가.
- `/lecture`·`/lecture/:moduleId` 모두 `LectureNotes`가 처리(딥링크 유지). `LectureModule.jsx` 제거.

## 2026-06-11 · 갤러리 7모듈화 + 학습강의안 모듈별 페이지 고도화

- **갤러리**: 9개·카테고리 필터 → **7개 모듈 결과물**(M1~M7 각 1개)로 정리.
  카드에 모듈 배지/제목, 미션 링크 정렬. 카테고리 필터 제거.
- **학습강의안 고도화**: 단일 스크롤 → **모듈별 개별 페이지**(`/lecture/:moduleId`).
  - 인덱스(`/lecture`): TIMELINE 사이드내비 + 모듈 카드(목표·세션·열기).
  - 상세(`LectureModule.jsx`): 세션마다 **핵심 개념(용어+설명) · 실습 절차(번호 스텝) ·
    프롬프트 예시(코드 블록) · 흔한 실수 · 결과물** + 도구 가이드·완료 체크리스트·이어보기.
  - 상세 콘텐츠는 `src/config/lectures.js`(`lectureDetails`)에 기술적으로 작성(7모듈×2세션).
  - 모듈 간 이전/다음 이동. 번들 ~299KB.

---

## 2026-06-11 · 공식 커리큘럼(7모듈) 반영

휴넷 공식 차시(7개 모듈, 합계 8H)로 커리큘럼 전면 교체. 기존 임의 설계(4모듈) 폐기.

- **7개 모듈** (M1~M6 각 1.0H + **M7 2.0H** = 8H):
  M1 업무효율화의 이해 · M2 홍보 기획(프롬프트) · M3 콘텐츠 작성 ·
  M4 시각자료 제작 · M5 UI 시안(**Google Stitch**) · M6 페이지 구성 · M7 반복업무 자동화.
- **8교시 배치**: M1~M6 → 1~6교시, M7 → 7~8교시(2H). 50분 단위 시간표 유지.
- 각 세션 학습목표·핵심개념·대면실습·도구를 공식 세부내용에 맞춰 재작성(14세션).
- **도장깨기 8미션**을 M2~M6 핵심 결과물에 재정렬(메시지정의→프롬프트→문안→SNS/카드뉴스
  →시각자료→카드뉴스세트→UI시안→페이지구성). 갤러리도 Google Stitch 등으로 갱신.
- 표시에 모듈번호(M1~M7)·시간(1.0H/2.0H) 추가. 빌드 정상(7모듈/14세션).

---

## 2026-06-11 · 상단 메뉴 6종 재구성 + 학습강의안 신설

상단 네비게이션을 **About / 커리큘럼 / 학습강의안 / 갤러리 / 도장깨기 / 커뮤니티** 6종으로 정리.

- **About**(`pages/About.jsx`, 신규) — 과정 소개·기대효과·대상 + 일정/장소 + 준비물(커리큘럼에서 이동).
- **커리큘럼**(`Curriculum.jsx`) — 하루 타임테이블 목차. 하단에 "학습강의안 보기" CTA.
- **학습강의안**(`pages/LectureNotes.jsx`, 신규) — **시간대별 학습자료 전문(全文)**.
  좌측 타임라인 사이드 내비 + 교시별 본문에 세션마다 학습목표·핵심개념·대면실습·도구를 펼침.
  커리큘럼이 "목차"라면 학습강의안은 "강의안 본문". (`/lecture`)
- **커뮤니티**(`pages/Community.jsx`, 신규) — 외부 채널 안내(오픈채팅/결과물공유/자료실) + FAQ 아코디언.
- 네비 데이터는 `site.js`의 `nav` 배열로 일원화. 헤더는 6개 + 모바일 가로 스크롤 대응.
- `site.js`에 `about`·`community` 데이터 추가. 빌드 정상(57 modules).

---

## 2026-06-11 · 대면 워크숍 전환 + Font Awesome

### 변경 동기
실제 휴넷 과정이 **동영상 강의가 아니라 오프라인 집합교육(1일 워크숍, 9/30 09:00–18:00,
휴넷캠퍼스 비전룸)**임을 반영. 영상 시청형 → **대면 강의 학습자료**로 전환. 더불어 사이트
전반의 이모지를 **Font Awesome 6.5.2 아이콘**으로 교체.

### 한 일
- **이모지 → Font Awesome**: `components/Icon.jsx` 래퍼 추가, CDN(index.html) 연결.
  features/stamps/gallery/brand/schedule/bring/material 전부 FA 클래스로 교체.
  (본문 속 흐름 화살표 `→` 는 의미 텍스트라 유지)
- **영상 → 학습자료**: `Lesson.jsx` 전면 재작성 — YouTube 플레이어 제거,
  세션별 **학습목표 · 핵심개념 · 대면 실습 · 사용 도구 · 도장깨기 연결**로 구성.
- **커리큘럼 = 하루 타임테이블**: 4교시 15세션, 교시별 시간대(09:00–18:00) + 준비물 안내.
- **site.js**: 각 세션에 objectives/keypoints/practice/tools 추가, course.schedule·bring 추가,
  히어로를 "하루 만에 짓는 AI 홍보 실무" 대면 워크숍 톤으로.
- **정적화**: 로그인/결제/진도 대시보드 미사용 → Auth·Progress 컨텍스트, lib(supabase/db/payment),
  Login·YouTube 컴포넌트 삭제. App에서 Provider 제거. 도장깨기만 localStorage 사용.
- 결과: 번들 465kB → **259kB**(Supabase 트리셰이킹), 빌드 정상.

---

## 2026-06-11 · 초기 구축

### 개요
휴넷 인재키움 과정 **「AI로 만드는 홍보 실무 — 콘텐츠·이미지·페이지까지 자동화 완성」**(단과·초급·8시간)을
온라인 학습사이트로 구축. 베이스 템플릿은 [`templete03`](https://github.com/aebonlee/templete03)
(React 19 + Vite 7 + Supabase, 다크블루 아트디렉션 + 형광펜 시그니처).

- 리포: `aebonlee/contents` (초기 `content` → 복수형 `contents`로 변경)
- 도메인: `contents.dreamitbiz.com` (CNAME)
- 배포: GitHub Actions → **gh-pages 브랜치** (`peaceiris/actions-gh-pages`, main push 자동)
- 테이블 접두사: `cnt_` (DreamIT 공용 Supabase 프로젝트 공유)

### 콘셉트 결정
- **출결/결제 대시보드는 제외** — 강의 내용 전달에 집중.
- 대신 두 가지 핵심 장치 추가:
  1. **도장깨기(Stamp Rally)** — 강의를 8개 실습 미션으로 쪼개 직접 만들며 도장을 찍는다.
     로그인 없이 `localStorage`에 진행 저장. 듣고 끝내지 않고 손으로 결과물을 남겨 체화.
  2. **갤러리(Gallery)** — "이걸 만들게 됩니다"를 한눈에. 콘텐츠/이미지/페이지 3카테고리 필터.
- 모든 레슨 `free: true` — 콘텐츠를 막지 않고 누구나 열람.

### 커리큘럼 (과정명 기준 설계, 4모듈 15레슨 ≈ 8시간)
> 휴넷 상세 차시는 로그인 비공개 영역이라 추출 불가 → 과정명 기준으로 설계. 실제 강의 준비 시 `videoId`만 교체.
1. **AI 홍보 실무 시작하기** — 도구 지도 + 프롬프트 기본기 (3레슨)
2. **AI 콘텐츠 자동화** — 카피·SNS·보도자료 (4레슨)
3. **AI 이미지 자동화** — 생성형 비주얼·카드뉴스·썸네일 (4레슨)
4. **페이지 & 워크플로 자동화 완성** — 노코드 페이지 + 자동화 루틴 (4레슨)

### 도장깨기 8미션
타깃 한 줄 정의 → AI 카피 3종 → SNS 캡션 세트 → 보도자료 초안 →
브랜드 이미지 → 카드뉴스 1세트 → 랜딩페이지 배포 → 자동화 루틴.

### 변경 파일
- `src/config/site.js` — 과정 콘텐츠 전면 교체 (course/hero/modules/stamps/gallery)
- `src/pages/Stamps.jsx` — 신규(도장깨기, localStorage)
- `src/pages/Gallery.jsx` — 신규(갤러리, 카테고리 필터)
- `src/pages/Home.jsx` — 가격 섹션 제거, 도장깨기·갤러리 미리보기 추가
- `src/components/Header.jsx` — 네비 교체(커리큘럼/도장깨기/갤러리), 로그인 제거
- `src/components/Footer.jsx`, `src/styles/layout.css` — 신규 컴포넌트 스타일
- `index.html`, `package.json`, `vite.config.js`, `.env.*` — content 메타/접두사
- `.github/workflows/deploy.yml` — gh-pages 브랜치 배포로 전환
- `supabase/schema.sql` — 접두사 `tpl_` → `cnt_`

### 빌드/배포
- `npm run build` 정상 (100 modules, 에러 없음).
- main push → Actions가 gh-pages 브랜치로 배포. Pages 소스 = gh-pages 브랜치 root.

### TODO
- [ ] 실제 강의 영상 확보 시 `modules[].lessons[].videoId` 교체
- [ ] 갤러리 placeholder → 실제 결과물 이미지 교체
- [ ] (선택) Supabase `cnt_` 테이블 생성 — 진도·노트 기능 활성화 시
